/*
interface ILifeOpts {
  bits: number;
  interval: number;
}

interface IBasePiece {
  x: number;
  y: number;
  height: number;
  width: number;
}

interface IPiece extends IBasePiece {
  isAlive: boolean;
  color: string;
  key: number; // Function of bits, x & y
}

type IPieces = { [key: number]: IPiece }
*/
